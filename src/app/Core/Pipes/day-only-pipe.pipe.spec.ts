import { DayOnlyPipePipe } from './day-only-pipe.pipe';

describe('DayOnlyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DayOnlyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
